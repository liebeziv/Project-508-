可疑文件
    File Hash
    
    Hash（MD5 SHA1 SHA256）是恶意软件的唯一通用标识符
    文件名不是可靠的标识符
    在线扫描器（如VirusTotal）可以使用Hash来查询文件，而无需上传文件本身，这对于避免泄露敏感数据非常重要
    注意！：没有检测结果并不意味着文件是安全的。在线扫描器可能会储存上传的文件。

    恶意软件命名：
    Hash虽然是唯一的，但是缺乏描述性
    为了更好的描述恶意软件，行业使用命名约定，比如CARO命名

    文件结构：
    非结构化文件：没有定义的结构，比如.txt .html
